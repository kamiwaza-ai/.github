# Default reviewer for all files across the org.
# This requests review from @jxstanford on every PR but does NOT
# require their approval to merge (require_code_owner_review is false
# in the org ruleset).
* @jxstanford
